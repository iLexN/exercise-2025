version: '3.8'

services:
  nest-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: local
      DATABASE_USER: test
      DATABASE_PASSWORD: test
    command: npm run start:dev
    volumes:
      - .:/usr/src/app
